[build]
  publish = "."
  command = "echo 'No build step required'"

[[redirects]]
  from = "/api/*"
  to = "https://your-railway-backend-url.herokuapp.com/api/:splat"
  status = 200

[build.environment]
  NODE_VERSION = "18"